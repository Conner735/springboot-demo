apiVersion: v1
kind: Service
metadata:
  name: springboot-demo
  namespace: default
spec:
  selector:
    app: springboot-demo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: NodePort
